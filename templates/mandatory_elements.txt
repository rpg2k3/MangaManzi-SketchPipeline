============================================================
MANDATORY OUTPUT ELEMENTS — ALL THREE MUST BE PRESENT
============================================================

The output is INCOMPLETE and INVALID if ANY of the following are missing.
Treat these as hard requirements, not stylistic preferences. Do not skip,
abbreviate, or omit any of them. They must all be clearly visible in the
final image.

REQUIREMENT 1 — RED LINE OF ACTION:
A single continuous RED line drawn ON TOP OF the figure, tracing the
primary rhythm curve from the crown of the head through the spine and
weight-bearing support down to the ground. The line must be:
  • RED in color (not blue, not black, not pencil)
  • Drawn AS AN OVERLAY on top of the blue construction figure
  • Continuous and clearly visible — not faint, not dashed, not optional
  • Following the pose-specific curve described in the POSE RHYTHM section

REQUIREMENT 2 — NUMBERED HEAD-HEIGHT GRID ON LEFT MARGIN:
Horizontal guideline marks numbered along the LEFT MARGIN of the page,
matching the head count for this archetype. The grid must include:
  • Numbered markers from 0 (ground) to {HEAD_COUNT} (crown),
    labeled with their actual numbers (0, 1, 2, 3, ... up to HEAD_COUNT)
  • A dashed line at marker 0 representing the ground plane
  • Light blue horizontal guidelines extending across the figure
    at each numbered marker
  • The figure positioned so its crown touches marker {HEAD_COUNT}
    and soles touch marker 0

REQUIREMENT 3 — A4 ASPECT RATIO:
The output must fit cleanly in A4 portrait or landscape ratio as specified
in the ORIENTATION line of this prompt. Do not crop the figure, the
line of action, or the head-height grid.

If the output is missing the red line of action, missing the numbered
grid markers on the left margin, or has the figure cropped, the output
is REJECTED. Include all three elements visibly.
============================================================